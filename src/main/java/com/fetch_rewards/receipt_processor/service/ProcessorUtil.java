package com.fetch_rewards.receipt_processor.service;

import com.fetch_rewards.receipt_processor.entity.Receipt;

public class ProcessorUtil {

    public static long getPoints(Receipt receipt) {
        long points = 0;

        return points;
    }
}
